'use client';

import Logo from './logo';

function Header() {
  return (
    <header className="flex w-full items-center justify-between">
      <Logo />
    </header>
  );
}

export default Header;
